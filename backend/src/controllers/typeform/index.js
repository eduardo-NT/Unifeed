/**
 * Typeform Controllers Index
 * Entry point for all Typeform-related controllers
 */

const typeformController = require('./typeformController');
const authController = require('./authController');

module.exports = {
  typeformController,
  authController
};
